# Azure Blob Storage container for test-app.
# Creates a container named "test-app-data" in the cluster's default storage account.
# The container name matches the claim name.
apiVersion: storage.p6m.dev/v1alpha1
kind: BlobStorage
metadata:
  name: test-app-data
  labels:
    p6m.dev/app: test-app
spec: {}
