# Base Kustomization for PlatformApplication
# Variables: None - this is static

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application.yaml
  - blobstorage.yaml
  - identityroleassignment.yaml

configurations:
  - application_customizations.yaml
